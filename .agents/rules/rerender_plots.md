# Plot and Render Workflow Rules

- ALWAYS re-run example scripts and re-generate plots / animations by default whenever changes are made or when evaluating/diagnosing visuals.
- Never wait for the user to ask or beg for a re-render. Produce the updated plot / GIF artifact immediately so the user can see the actual rendered visual.
