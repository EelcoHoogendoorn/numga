# Formatting Rules

- DO NOT use LaTeX formatting or math delimiters ($ or $$) anywhere in responses. The chat UI does not render LaTeX.
- Always use plain text, ASCII, or standard Unicode characters (e.g. x^2, theta, sum, sqrt, ->).
