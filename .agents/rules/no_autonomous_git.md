# Git Commit & Push Rules

- **NEVER commit or push without explicit user command**: Never run `git commit` or `git push` autonomously.
- Always leave all modifications, created files, and renames uncommitted in the working tree for user review.
- Only run `git commit` or `git push` when the user explicitly commands it.
